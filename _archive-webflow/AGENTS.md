Attention important : 

le code est découpé en plusieurs embed par page
webflow a une limite de 50 000 Caractères par embed

l'utilisateur est novice, donc quand on fait une feature ou un fix, on lui redonne tout l'embed correspondant à copier coller pour remplacer l'ancien embed

**NE JAMAIS modifier les `src` ou `href` des images / assets.** Toujours garder exactement les URLs (CDN Webflow, etc.) telles qu'elles sont dans l'embed que l'utilisateur a fourni. Pas de `images/`, pas d'Unsplash à la place des vrais assets.
